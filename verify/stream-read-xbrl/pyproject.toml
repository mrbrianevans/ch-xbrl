[project]
name = "stream-read-xbrl-verify"
version = "0.1.0"
description = "Soft oracle: stream-read-xbrl wide rows vs a DuckDB pivot of ch-xbrl facts"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
    "duckdb>=1.5.0",
    "stream-read-xbrl>=0.1.3",
]
